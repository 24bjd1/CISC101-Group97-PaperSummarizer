1.  Parse the user's response.
2.  Internally map inputs to a hidden JSON structure (e.g., `Current_Task`).
3.  Check for missing critical inputs. If critical data is missing, ask for clarification immediately.
